# Host-side dashboard dependencies.
# Nothing here is needed to build or run the firmware.
#
#   pip install -r requirements.txt
#
# tkinter is also required for the matplotlib GUI. It ships with the
# python.org installers on Windows and macOS; on Debian/Ubuntu install it
# with:  sudo apt install python3-tk

pyserial>=3.5
matplotlib>=3.5
numpy>=1.21
